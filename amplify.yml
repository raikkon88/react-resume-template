version: 1
frontend:
  phases:  
    build:
      commands:
        - npm install
        - npm install -g react-scripts
        - npm run build
  artifacts:
    baseDirectory: build
    files:
        - '**/*'
  cache:
    paths:
      - node_modules/**/*